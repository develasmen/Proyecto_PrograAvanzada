@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Ups! Algo salio mal";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm border-0">
                <div class="card-body text-center p-5">
                    <div class="display-4 text-danger mb-3">
                        <i class="fas fa-bug"></i>
                    </div>
                    <h2 class="mb-3">Tuvimos un problema al procesar tu solicitud</h2>
                    <p class="text-muted">
                        Ocurrio un error interno (codigo 500). Estamos trabajando para resolverlo. 
                        Intenta nuevamente en unos minutos o regresa al inicio para continuar con otras tareas.
                    </p>
                    <div class="d-flex justify-content-center gap-2 mt-4 flex-wrap">
                        <a href="javascript:location.reload()" class="btn btn-outline-secondary">
                            <i class="fas fa-sync-alt"></i> Reintentar
                        </a>
                        <a href="@Url.Action("Index", "Home")" class="btn btn-primary">
                            <i class="fas fa-home"></i> Ir al inicio
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
