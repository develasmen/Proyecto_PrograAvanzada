@model Inventario.Abstracciones.ModelosParaUI.ClienteDto
@{
    ViewBag.Title = "Confirmar Eliminación";
}
<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-9 col-lg-8">
            <h2 class="mb-4">@ViewBag.Title</h2>
            <div class="alert alert-danger">
                <strong>¡ATENCIÓN!</strong> ¿Está seguro que desea **ELIMINAR** este cliente de forma permanente?
            </div>
            <div class="card shadow-sm">
                <div class="card-header bg-secondary text-white">
                    <h4 class="mb-0">Detalles del Cliente a Eliminar</h4>
                </div>
                <div class="card-body">
                    <dl class="row mb-0">
                        <dt class="col-sm-4">@Html.DisplayNameFor(model => model.CedulaJuridica)</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.CedulaJuridica)</dd>

                        <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Nombre)</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Nombre)</dd>

                        <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Correo)</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Correo)</dd>

                        <dt class="col-sm-4">@Html.DisplayNameFor(model => model.Telefono)</dt>
                        <dd class="col-sm-8">@Html.DisplayFor(model => model.Telefono)</dd>
                    </dl>
                </div>
            </div>
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(model => model.Id)
                <div class="d-flex justify-content-start gap-3 mt-4">
                    <input type="submit" value="Sí, Eliminar" class="btn btn-danger btn-lg" />
                    @Html.ActionLink("Cancelar", "ListarClientes", "Clientes", null, new { @class = "btn btn-secondary btn-lg" })
                </div>
            }
        </div>
    </div>
</div>